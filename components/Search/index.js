import React from "react";

// Styling
import { TextStyle } from "./styles";

// this isnt doing anything...
const Search = ({ navigation }) => {
  return <TextStyle>Search</TextStyle>;
};

export default Search;
